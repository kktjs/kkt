{
  "name": "typescript-example",
  "version": "1.0.0",
  "description": "typescript example",
  "private": true,
  "scripts": {
    "start": "kkt start",
    "build": "kkt build",
    "bundle": "kkt build --bundle --no-emptyDir",
    "bundle:watch": "kkt build --bundle --no-emptyDir -w",
    "bundle:min": "kkt build --bundle=min --no-emptyDir",
    "test": "kkt test --env=jsdom",
    "test:coverage": "kkt test --env=jsdom --coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kktjs/kkt-next.git"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "classnames": "^2.2.6",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "@kkt/loader-less": "^1.0.6",
    "@types/classnames": "^2.2.8",
    "@types/jest": "^24.0.23",
    "@types/react": "^16.9.13",
    "@types/react-dom": "^16.9.4",
    "kkt": "file:../../"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
